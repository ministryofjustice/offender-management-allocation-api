apiVersion: v1
kind: Service
metadata:
  name: allocation-api
  namespace: offender-management-production
spec:
  ports:
  - port: 3000
    name: http
    targetPort: 3000
  selector:
    app: allocation-api
